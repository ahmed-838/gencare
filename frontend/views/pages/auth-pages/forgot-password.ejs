<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/auth-components/authPages.css">
    <title>Forgot Password</title>
    <style>
        .container {
            display: flex;
            justify-content: flex-end; /* جعل النموذج في الجانب الأيمن */
            align-items: flex-end; /* محاذاة النموذج في الأسفل */
            height: 100vh;
            background-color: #f7f7f7;
        }

        .form-container {
            margin-bottom: 165px;
            margin-right: 20px;
        }

        .input-field {
            grid-template-columns: 100% 100%;

        }

        .forgot-password {
            color: #007bff;
            text-decoration: none;
        }

        .forgot-password:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .container {
                justify-content: center; /* محاذاة النموذج في المنتصف في الشاشات الصغيرة */
                align-items: flex-end; /* محاذاة النموذج في الأسفل */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <div class="form-header">
                <h3>Forgot Your Password?</h3>
            </div>
            <div class="form-body">
                <p>Enter your email address .</p>
            </div>
            <form action="/auth/reset-password" method="POST">
                <div class="input-field">
                    <input type="email" name="email" placeholder="Email" required>
                </div>
                <input type="submit" value="Send Reset Link" class="btn solid">
            </form>
            <p><a href="/auth/login" class="forgot-password">Back to Login</a></p>
        </div>
    </div>
</body>
</html>